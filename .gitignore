node_modules/
dist/
dev-dist/
.DS_Store
*.local
.vite/

# Personal itinerary with booking details — never commit
content/reference/

# CI needs the lockfile (overrides global ignore)
!package-lock.json
